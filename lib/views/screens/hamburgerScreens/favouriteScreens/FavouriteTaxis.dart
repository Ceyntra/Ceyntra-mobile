import 'package:flutter/material.dart';

class FavouriteTaxisScreen extends StatelessWidget {
  // const FavouriteTaxisScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        Column(
          children: [
            Container(
              width: 100,
              height: 100,
              color: Colors.redAccent,
            ),
          ],
        )
      ],
    );
  }
}
